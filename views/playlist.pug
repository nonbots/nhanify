extends playlists

block list 
  .listHeader
    .headerLeft
      p(class="username") #{playlist.songs.length} song(s)  
      p(class="username") Total Duration: 
    if (user !== undefined)
      a(href=`/${playlistType}/playlist/${playlistId}/contributors`) Contributors
  .iframe
  .addDiv
    form(class="addForm" action=`${playlistType}/playlist/${playlist.id}/addSong` method="post")
      label(for="url") Youtube Url
      input(id="url" type="text" name="url") 
      label(for="title") Title
      input(id="title" type="text" name="title") 
      input(class="deleteBtn" type="submit" value="Add")
  .cols
    .label
      p(class="username") No.
    .label
      p(class="username") Title
    .label
      p(class="username") Added By
    .label
      p(class="username") Duration
    .label
block values
  .content
    .playListWrap
      each song in playlist.songs
        a(href=`/playlist/${song.id}`) 
          div(class="songCard")
            .val
              p= playlist.songs.indexOf(song) + 1
            .val
              p= song.title   
            .val
              p= song.added_by
            .val
              p= song.formattedDuration
            .val
              if (user !== undefined)
                form(class="delete" action=`/playlists/delete/${playlist.id}` method="post")
                  input(class="deleteBtn" type="submit" value="❌")

