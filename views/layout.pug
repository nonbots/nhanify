doctype html
html(lang="en")
  head 
    title Nhanify
    meta(charset="UTF-8")
    link(rel="stylesheet" href="/assets/css/reset.css")
    link(rel="stylesheet" href="/assets/css/myStyle.css")
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet")
    script(src="/assets/javascript/main.js")
  body
    .bWrap
      .navTopWrap 
        .stack1
          .navTopLeft
            if user !== undefined 
              .home
                a(class="logo" href="/playlists/your") N
              p(class="username") #{user.username}
            else 
              .home
                a(class="logo" href="/playlists/public") N
          .navTopRight
            if (user !== undefined)
              form(class="buttonForm" action="/signout" method="post")
                input(type="submit" value="Sign Out")
            else
              a(href="/signup") Sign Up 
              a(href="/login") Sign In
        .stack2
          if (playlistType === "public")
            a(class="hiNavTab" href="/playlists/public") Public Playlists 
          else
            a(class="navTab" href="/playlists/public") Public Playlists 
          if (user !== undefined)
            if (playlistType === "owned")
              a(class="hiNavTab" href="/playlists/your") Your Playlists 
            else 
              a(class="navTab" href="/playlists/your") Your Playlists 
            if (playlistType === "contribution")
              a(class="hiNavTab" href="/playlists/contributing") Contributing Playlists
            else
              a(class="navTab" href="/playlists/contributing") Contributing Playlists
      main
          block main
